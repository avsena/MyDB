'use strict';

module.exports = {
  async up (queryInterface, Sequelize) {
    /**
     nome da mae, idade usuaria, nome do representante familiar, nis, etc..
     */
  },

  async down (queryInterface, Sequelize) {
    /**
     * Add reverting commands here.
     *
     * Example:
     * await queryInterface.dropTable('users');
     */
  }
};
